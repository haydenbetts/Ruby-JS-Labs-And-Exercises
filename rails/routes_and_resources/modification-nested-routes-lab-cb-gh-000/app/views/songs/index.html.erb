<ul>
<% @songs.each do |song| %>
  <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
<br>

<% if @artist %>
    <%= link_to "Add New Song for Artist", new_artist_song_path(@artist) %>
<% end %>
