<%= form_for @song  do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.hidden_field :artist_id %>

  <!-- <% if @artist %>
    <%= f.label :artist_name %>
    <%= f.text_field :artist_name %>
  <% else %>
    <%= collection_select :song, :artist_id, Artist.all, :id, :name %>
  <% end %> -->

  <!-- helper is the above factored out -->
  <%= artist_select(@artist, @song) %>

  <br />
  <%= f.submit %>
<% end %>
